diff --git "a/..\\openemr-8.2.0\\interface\\forms\\procedure_order\\common.php" "b/patch\\openemr\\interface\\forms\\procedure_order\\common.php"
index 6052405..19917a6 100644
--- "a/..\\openemr-8.2.0\\interface\\forms\\procedure_order\\common.php"
+++ "b/patch\\openemr\\interface\\forms\\procedure_order\\common.php"
@@ -731,11 +731,15 @@ if (!empty($row['lab_id'])) {
 
             let title = <?php echo xlj("Find Procedure Order"); ?>;
             // This replaces the previous search for an easier/faster order picker tool.
+            // El tipo de orden (procedure_order_type) se envía al popup para que,
+            // cuando sea 'imaging', se muestre únicamente el catálogo de imágenes.
+            let otype = (typeof f.procedure_type_names !== 'undefined') ? f.procedure_type_names.value : '';
             const params = new URLSearchParams({
                 formid: <?php echo js_escape($formid); ?>,
                 formseq: formseq,
                 labid: f.form_lab_id.value,
-                order: f[ptvarname].value
+                order: f[ptvarname].value,
+                otype: otype
             });
             dlgopen('../../orders/find_order_popup.php?' + params,
                 '_blank', 850, 500, '', title);
